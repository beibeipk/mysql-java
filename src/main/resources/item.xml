<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.thoughtworks.learning.core.ItemsRepository" >

    <resultMap id="ItemMapper" type="Items">
        <id property="barcode" column="barcode" />
        <result property="name" column="name" />
        <result property="unit" column="unit" />
        <result property="price" column="price" />
    </resultMap>

    <!--<resultMap id="ItemsBarcodeMapper" type="Items">-->
        <!--<id property="barcode" column="barcode" />-->
    <!--</resultMap>-->

    <select id="findItems" resultMap="ItemMapper">
        select * from Allitems;
    </select>

    <!--<select id="findItemsBarcode" resultMap="ItemsBarcodeMapper">-->
        <!--select barcode from Allitems;-->
    <!--</select>-->

    <!--<select id="getBarcodeByPromotions" resultMap="ItemsBarcodeMapper">-->
        <!--<include refid="selectPromotionsItemsBarcode" />-->
        <!--select barcode form Allitems where promotions=#{type};-->
    <!--</select>-->
</mapper>